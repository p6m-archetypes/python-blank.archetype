---
description: "Python blank archetype — platform CI/CD + Kubernetes wiring with no application code. Profiles: REST API, Background Worker, Scheduled Task."
authors: ["p6m-archetypes"]
languages: ["Python"]
tags: ["Python", "uv", "Minimal", "REST API", "Worker", "Scheduled Task", "Brownfield"]

requires:
  archetect: "2.0.5"
